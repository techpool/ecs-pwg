const ServiceConfig = {
    USER: {
        ENDPOINT: '/users/v2.0'
    },
    PRATILIPI: {
        ENDPOINT: '/pratilipis'
    },
    AUTHOR: {
        ENDPOINT: '/authors'
    },
    EVENT: {
        ENDPOINT: '/events'
    }
};

module.exports = ServiceConfig;
